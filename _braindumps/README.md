# _braindumps/

Braindump sync folder for overnight-build automation.

Files are synced here from `~/.copilot/braindumps/{date}/` by the
`overnight-sync.sh` script. The GitHub Actions workflow detects new
files and triggers the build pipeline.

**Do not edit files here directly.** Source of truth is `~/.copilot/braindumps/`.

