- `lib/factory-core.ts:25-44,406-412`, `lib/factory-capabilities.ts:11-35`, `.pi/extensions/pi-gstack/index.ts:781-784,867-893` — Medium: `commandSafetyProfile` is now typed and persisted, but the promised `non-destructive-write` boundary is still not enforced by the factory runtime/tool layer. The only runtime use is rejecting `read-only` plans and checking for exact equality before dispatching `/skill:gstack-qa`; the destructive-command restrictions themselves are carried only in prompt text. Impact: a prompt-injected or misbehaving QA-fix run still has full local repo write capability and can execute destructive commands contrary to the advertised safety contract. Confidence: high. Recommended fix: make `commandSafetyProfile` a first-class runtime control by enforcing a destructive-command denylist / allowlist at dispatch time (or routing QA-fix through an enforced `careful`/sandbox policy) and fail closed when the runtime cannot attest to that enforcement.
