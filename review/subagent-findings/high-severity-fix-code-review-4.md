- Warning — `.pi/extensions/pi-gstack/index.ts:209-229,801-806`, `lib/factory-qa-workflow.ts:40` — `/factory-qa-fix` now requires `safe-command-guard`, but the only Pi runtime used by that command never advertises that capability. As written, every `/factory-qa-fix` invocation is blocked before dispatch, so the command stays public while being impossible to run on this host. Either wire a real guard capability into `createPiReviewDispatchRuntime()` or hide/register the command only when that capability is actually available.
- Warning — `.pi/extensions/pi-gstack/index.ts:265-289,340-362,652-653`, `lib/factory-artifact-store.ts:44-45` — manual review capture, manual QA capture, and review auto-capture still write fixed `*-captured` artifacts before `appendValidated()` proves the run is still pending. A duplicate or racing completion can lose the event append yet still overwrite the already-accepted artifact on disk, leaving durable evidence out of sync with the event log. Validate before publishing the canonical artifact, or write to a temp/unique id and promote it only after the append succeeds.
