- `lib/factory-core.ts:406-411`, `lib/factory-qa-workflow.ts:63`, `.pi/extensions/pi-gstack/index.ts:865-868` — `qa-fix` still does not encode its required safety profile in the factory contract. `compileRunPlan()` treats any non-`read-only` profile as acceptable for write-capable workflows, even though `qa-fix` is explicitly “non-destructive local fixes” only; the stricter `non-destructive-write` check exists only inside the Pi runtime. A caller can therefore start/persist a `qa-fix` run with `commandSafetyProfile: 'release-action'`, and any non-Pi runtime that omits the ad-hoc check can execute the workflow under the wrong safety mode. Block this at the workflow/plan layer (for example, an allowed/required safety-profile field on the workflow or phase) and add a regression test for `qa-fix` + `release-action`.
- `lib/factory-event-store.ts:434-438` — persisted run-plan validation still accepts `policy.commandSafetyProfile` being absent, even though `PolicySpec` now makes it required. That means a legacy or tampered `run_started` envelope can bypass the new typed safety contract at read time and rely on later runtime-specific checks (if any) instead of failing closed. Require a valid `commandSafetyProfile` during event-log parsing, or add an explicit migration path for older runs, and cover it with a parse/read regression test.
